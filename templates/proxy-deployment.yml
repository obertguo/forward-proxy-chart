apiVersion: apps/v1
kind: Deployment
metadata:
  name: iac-proxy-deployment
  labels:
    app: iac-proxy-deployment
    chart: iac-proxy-deployment
    release: iac-proxy-deployment
    version: 1.0.0
    deployment-type: api
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: iac-proxy-deployment
  template:
    metadata:
      name: iac-proxy-deployment
      labels:
        app: iac-proxy-deployment
        chart: iac-proxy-deployment
        release: iac-proxy-deployment
        pod-type: api
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: iac-proxy-deployment
        image: vimagick/tinyproxy:latest@sha256:72b441b95ee1e641af948f68f09492f9f795ead72b73954414e339168c98ad8c
        imagePullPolicy: IfNotPresent
        livenessProbe:
          tcpSocket:
            port: 8888
          initialDelaySeconds: 10
          timeoutSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 8888
          initialDelaySeconds: 10
          timeoutSeconds: 10
        resources:
          limits:
            memory: 4096Mi
            cpu: "2"
          requests:
            memory: 2048Mi
            cpu: "1"
        ports:
        - containerPort: 8888
        env:
        - name: USERNAME
          value: user
        - name: PASSWORD
          value: pass
        
